description: run scheduler


inputs:
  token:
    description: github token
    required: false
    default: ${{ github.token }}


runs:
  using: composite

  steps:
    - uses: li1ht2ay-3es/gi1-ac2io3s@keep-alive
      with:
        token: ${{ inputs.token }}
        auto: true
