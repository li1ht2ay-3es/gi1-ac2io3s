on:
  workflow_dispatch:
  schedule:
    - cron: '0 17 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/gi1-ac2io3s@cron
        with:
          token: ${{ secrets.token }}
